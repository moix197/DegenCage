# Knowledge Index

The map agents read first. One row per module/package: its single responsibility,
where it lives, and links to any decision or pattern doc. Keep rows terse —
this is a lookup table, not documentation. Retire rows that no longer point
anywhere real.

For package boundaries, dependency direction and data flow, read
[architecture](architecture.md) alongside this map.

## Modules

| Module / package | Responsibility (one line) | Path | Decisions / patterns |
| ---------------- | ------------------------- | ---- | -------------------- |
| `@degencage/web` | Next.js App Router UI + route handlers; owns everything server-side that isn't the rule engine | `apps/web` ([README](../apps/web/README.md)) | [hosting-and-growth-path](decisions/hosting-and-growth-path.md), [ui-framework](decisions/ui-framework.md) |
| `@degencage/rules` | The rule engine — pure, I/O-free, the product IP; owns the constitution document schema, `evaluateTrade()` (`daily_notional_usd`, `asset_tier_acquisition_usd`, `rolling_loss_usd`), and the exact-decimal USD helpers (`sumTradeUsd`, `sumRealizedLosses`) | `packages/rules` | [monorepo-package-shape](decisions/monorepo-package-shape.md), [constitution-schema](decisions/constitution-schema.md) |
| db | Drizzle schema, migrations, and the one pooled Postgres handle (`getDb()`). Tables: `users`, `wallets` (incl. `reconciled_through_slot`/`lots_built_through_slot`, two independent cursors), `siws_challenges`, `sessions`, `events`, `feature_flags`, `constitutions`, `constitution_pending_changes` (Phase 8's decrease-instant/increase-timelocked edits — `applied_at`/`voided_at` are its two terminal states), `trades` (incl. `is_round_trip_close`/`realized_loss_usd`), `token_prices`, `position_lots` (FIFO cost-basis lots, Phase 6), `admin_login_attempts` (Phase 9's login throttle counter, `server/admin/login-rate-limit.ts`) | `apps/web/src/server/db` | [migration-and-test-tooling](decisions/migration-and-test-tooling.md), [single-source-of-truth-database](decisions/single-source-of-truth-database.md), [reconciliation-idempotency](decisions/reconciliation-idempotency.md) |
| flags | `isFeatureEnabled()` — the single fail-closed kill-switch read path | `apps/web/src/server/flags` | [feature-flags-and-kill-switches](decisions/feature-flags-and-kill-switches.md) |
| observability | `logger` + `captureError` — the only import points for pino and Sentry — plus `recordEvent()`, the **sole** write path into `events`: the audit trail Phase 5 reads, server-stamped `observed_at`, transaction-aware | `apps/web/src/observability` | [observability-stack](decisions/observability-stack.md), [event-time-vs-observation-time](decisions/event-time-vs-observation-time.md) |
| auth | SIWS sign-in, rate-limited nonce issuance, session issue/revoke/`resolveSession()` | `apps/web/src/server/auth` ([README](../apps/web/src/server/auth/README.md)) | [rate-limit-forwarded-header-trust](decisions/rate-limit-forwarded-header-trust.md), [guarded-state-transition](patterns/guarded-state-transition.md) |
| wallet (client) | Browser half of sign-in: connect panel, SIWS dispatch, and the account-switch watcher's three change channels | `apps/web/src/client/wallet` (documented in the [auth README](../apps/web/src/server/auth/README.md)) | [wallet-account-switch-desync](decisions/wallet-account-switch-desync.md), [wallet-standard-ui-dependency](decisions/wallet-standard-ui-dependency.md) |
| constitution | Draft → commit → activate lifecycle of a trading constitution, the 20-minute commitment window, and (Phase 8) editing an *active* constitution: a decrease applies instantly, an increase is rate limited (by attempt, not success) and a 48h timelocked `constitution_pending_changes` row, applied lazily on app open (dashboard + edit page loads — no scheduler) and voided rather than applied if the limit's value moved since the increase was requested; decreasing **and** cancelling a pending increase are both always exempt from the rate limit. `/constitution/edit` mutates via inline Server Actions, not an API route | `apps/web/src/server/constitution` ([README](../apps/web/src/server/constitution/README.md)); UI at `apps/web/src/app/constitution`, `apps/web/src/app/constitution/edit` | [commitment-window-server-clock](decisions/commitment-window-server-clock.md), [constitution-schema](decisions/constitution-schema.md), [asymmetric-constitution-edits](decisions/asymmetric-constitution-edits.md), [server-actions-for-constitution-edit](decisions/server-actions-for-constitution-edit.md), [guarded-state-transition](patterns/guarded-state-transition.md) |
| chain | Wallet history ingestion: Helius pull → swap derivation from balance deltas → market-cap tier classification → FIFO cost-basis lot-matching (`lot-matching.ts`, pure) → `reconcileWallet()`, the pipeline that writes `trades`/`position_lots`. Triggered on app open via `POST /api/wallet/reconcile`; no worker, no cron. Curated mint sets (`lst-allowlist.ts`, `stablecoin-mints.ts`) are shared with derivation, pricing, *and* lot-matching's quote-leg exclusion so the lists cannot diverge | `apps/web/src/server/chain` | [chain-data-source](decisions/chain-data-source.md), [reconciliation-idempotency](decisions/reconciliation-idempotency.md), [asset-tier-by-market-cap](decisions/asset-tier-by-market-cap.md) |
| pricing | A swap's USD value: stablecoin leg at face, SOL leg from the shared `token_prices` minute cache, alt↔alt via Birdeye; unpriceable is `null`, never `0`. A *pre-trade* caller names the leg explicitly (`leg: 'sold' | 'bought'`) instead of letting liquidity order decide — post-execution reconciliation keeps the inferred order | `apps/web/src/server/pricing` | [usd-pricing-source](decisions/usd-pricing-source.md), [pre-trade-slippage-pricing](decisions/pre-trade-slippage-pricing.md) |
| rules (server) | The I/O half of rule evaluation: rolling-window trade queries feeding `packages/rules`' pure `evaluateTrade()`, and the dashboard's live allowances (daily notional, per-tier, and — Phase 6, gated behind `rules.loss_limit_enabled` — rolling loss) | `apps/web/src/server/rules` | [server-side-rule-evaluation](decisions/server-side-rule-evaluation.md), [usd-pricing-source](decisions/usd-pricing-source.md) |
| dashboard (server) | `buildDashboardState()` — the one function behind both the dashboard's initial SSR render and `GET /api/dashboard`'s poll response, so they can't drift — plus `loadViolationsFeed()`, the "we saw that" feed built from existing `rule.decision_recorded` events (no new rule logic, no new event type); baseline-sourced trades are re-checked and excluded even if their decision would read as a violation | `apps/web/src/server/dashboard`; UI at `apps/web/src/app/dashboard` | [ui-framework](decisions/ui-framework.md) |
| admin (Phase 9) | `server/admin/access.ts` — the one shared-secret comparison (`secretsMatch`), signed/expiring session-cookie format (`createAdminSessionCookieValue`/`verifyAdminSessionCookie`), and 32-char minimum-length (not entropy) enforcement (`getConfiguredAdminSecret`, `MIN_ADMIN_SECRET_LENGTH`) every admin-gated surface uses; `server/admin/login-rate-limit.ts`'s `attemptAdminLogin` atomically checks-and-records `POST /api/admin/login` attempts per client key (5 failed / 15 min, `admin_login_attempts` table, serialized via `pg_advisory_xact_lock` — closes both the online-guessing oracle a public login page otherwise leaves open, and the TOCTOU a naive check-then-insert would have), reaped by `login-attempt-reaper.ts` (mirrors `challenge-reaper.ts`). `POST /api/admin/login` exchanges `ADMIN_METRICS_SECRET` for the cookie via a plain HTML form at `/admin/login`, no client JS; `POST /api/admin/logout` clears it, only when the request already carries a valid session (CSRF-safe by construction — a cross-site `POST` can't supply a `sameSite: 'lax'` cookie). **All of it is hand-rolled, provisional and FROZEN** — no new features or endpoints on it; it gets replaced by a maintained auth library, not extended | `apps/web/src/server/admin`; UI at `apps/web/src/app/admin/login`, API at `apps/web/src/app/api/admin/login`, `apps/web/src/app/api/admin/logout` | [admin-auth-is-provisional](decisions/admin-auth-is-provisional.md), [admin-metrics-secret-gate](decisions/admin-metrics-secret-gate.md), [rate-limit-forwarded-header-trust](decisions/rate-limit-forwarded-header-trust.md) |
| metrics (Phase 9) | `buildMetricsSnapshot()` — every Phase 0 success signal, derived live from `events`/`constitutions`/`trades` (never a precomputed counter); the plan's own signal→event mapping table plus `getRepeatedLooseningAttemptStats`, added after Phase 8 shipped three event types the plan predates. Includes the ad hoc, never-persisted baseline-vs-live violation-frequency counterfactual, with `rolling_loss_usd` excluded from both sides (a baseline trade can never be loss-eligible), a fixed 90-day baseline window as its denominator (baseline's actual trade span is surfaced alongside it, informational only), and its own `unevaluableCount` surfaced rather than absorbed. Known, labeled (not fixed) methodological seam: baseline is evaluated against the constitution's *current* limits, live reflects whatever was active *at the time*. `rulesKept`/onboarding/activation/return-visit rates all render "no data" rather than "0%" for a zero-sample row. `/admin/metrics` (the page) and `GET /api/admin/metrics` (the route) are both gated — see the `admin` row above | `apps/web/src/server/metrics`; internal-only UI at `apps/web/src/app/admin/metrics`, API at `apps/web/src/app/api/admin/metrics` | [admin-metrics-secret-gate](decisions/admin-metrics-secret-gate.md), [baseline-counterfactual-metric](decisions/baseline-counterfactual-metric.md) |
| feedback (Phase 9) | The qualitative "killer signal" capture: `recordFeedbackPrompt()`/`recordFeedback()` write `feedback.prompt_shown`/`feedback.submitted`, opaque length-capped text (`context` is separately capped and closed-charset, `text` is length-capped only), never interpreted or scored — only read back verbatim by `listRecentFeedback()` for the metrics page's manual-review section. Only `recordFeedback` (`feedback.submitted`) is throttled, via `assertWithinConstitutionActionRateLimit` (the third consumer of that helper — see "Authenticated write surfaces" below); `recordFeedbackPrompt` (`feedback.prompt_shown`) is deliberately exempt — throttling it under the same shared budget as submissions made impressions under-report relative to submissions, inflating the apparent shown→submitted conversion rate. Length constants live in `server/feedback/constants.ts` (no server-only imports) so `dashboard/feedback-prompt.tsx` (`'use client'`) can import them without pulling in the rest of the module. Ships dark: `feedback.capture` is not yet seeded enabled. Surfaced in the dashboard only when the flag is on and the caller has a violation to show | `apps/web/src/server/feedback`; API at `apps/web/src/app/api/feedback`; UI at `apps/web/src/app/dashboard/feedback-prompt.tsx` | — |

> Add a row when a module lands. Don't pre-populate rows for paths that don't exist.

## Cross-cutting

| Concern | Where it's handled | Notes |
| ------- | ------------------ | ----- |
| Hosting / deploy | Vercel + hosted Postgres; managed services added additively, no VPS. The nonce limit **trusts the platform's forwarded headers** — re-verify before moving off Vercel | [hosting-and-growth-path](decisions/hosting-and-growth-path.md), [rate-limit-forwarded-header-trust](decisions/rate-limit-forwarded-header-trust.md) |
| Package layout | pnpm workspace; `apps/web` + `packages/rules`, not split further yet | [monorepo-package-shape](decisions/monorepo-package-shape.md) |
| Data store | one Postgres = source of truth; Redis / event store are never a copy of it | [single-source-of-truth-database](decisions/single-source-of-truth-database.md) |
| Rule enforcement | server-side only, server-authored timestamps | [server-side-rule-evaluation](decisions/server-side-rule-evaluation.md) |
| Rule state transitions | one `UPDATE … WHERE <precondition> … RETURNING`, never SELECT-then-UPDATE; zero rows means the precondition failed, not that the request did | [guarded-state-transition](patterns/guarded-state-transition.md) |
| Rule deadlines & timelocks | written and compared with the **database's** `now()` — an app-server `Date` drifts by inter-instance clock skew. Client countdowns are display only. Applying a due timelock (Phase 8's 48h limit-increase delay) is lazy on app open, never a scheduler — and re-validates the value it's about to change hasn't moved since the deadline was set, voiding rather than blindly applying if it has | [commitment-window-server-clock](decisions/commitment-window-server-clock.md), [asymmetric-constitution-edits](decisions/asymmetric-constitution-edits.md) |
| Time & history | `occurred_at` (block) vs `observed_at` (detection); stats derived, never counters | [event-time-vs-observation-time](decisions/event-time-vs-observation-time.md) |
| Chain history ingestion | Helius `getTransactionsForAddress` only; a trade is **derived by us** from net balance deltas, never read off a parsed-swap API. Excluded candidates (wrap/unwrap, LST swaps, pure sends/receives) are persisted with a reason, not dropped | [chain-data-source](decisions/chain-data-source.md) |
| Money math | Exact decimal strings via `BigInt` everywhere — pricing, rolling sums, rule evaluation. Never a float. `usd_value NUMERIC(38,12)`, nullable; an unpriceable trade is `null` and **never `0`**, and `null` propagates through every total and verdict | [usd-pricing-source](decisions/usd-pricing-source.md) |
| Reconciliation safety | Idempotent by `(wallet_id, signature)` + `ON CONFLICT DO NOTHING`; per-batch row lock with external pricing kept outside the transaction; cursor advanced only via `GREATEST` to a fully-persisted slot; `baseline_completed_at` is the sole "backfill finished" signal; `lots_built_through_slot` is a second, independent cursor for FIFO lot-matching, backfilled from `trades` (not Helius) when it trails `reconciled_through_slot` | [reconciliation-idempotency](decisions/reconciliation-idempotency.md) |
| Asset tiers | Graded by **market cap** (`STABLE`/`LARGE_CAP`/`MID_CAP`/`SMALL_CAP`/`MICRO_CAP`), never by asset identity. Stamped onto the trade at classification time and never recomputed; unlisted / no `mcap` / flag off all fail closed to `MICRO_CAP` + `classification: 'unknown'`, and classification never throws into the reconcile pipeline | [asset-tier-by-market-cap](decisions/asset-tier-by-market-cap.md) |
| FIFO lot-matching / loss limit | `lot-matching.ts` (pure): draws down a mint's oldest open lot(s) first on disposal, opens a new lot on acquisition; quote mints (SOL/LST/stablecoin) never tracked as a position. A close is loss-limit-eligible only if **every** consumed lot, and the close itself, is after the constitution's `activated_at` — one mixed or pre-activation lot excludes the whole close, never split/prorated ("partial coverage", disclosed in the authoring UI and the status page, not implied as full P&L). Gated end-to-end by `rules.loss_limit_enabled`: off, `evaluateTrade`'s `rolling_loss_usd` case returns `unevaluable('loss_matching_disabled')` (never a false "$0 lost"), and the status page shows the limit as unknown-status rather than clean | [reconciliation-idempotency](decisions/reconciliation-idempotency.md), [constitution-schema](decisions/constitution-schema.md) |
| Baseline vs live trades | The 90-day first-connect backfill is a private record: never evaluated, never in a rolling window, no decision or exclusion events, never user-facing. Enforced in `reconcile-wallet.ts` and in `loadWindowedTrades`' predicate. One narrow, internal-only exception: Phase 9's admin metrics evaluate baseline trades ad hoc, in memory, purely to compute a never-persisted, never-user-facing counterfactual — see [baseline-counterfactual-metric](decisions/baseline-counterfactual-metric.md) | [reconciliation-idempotency](decisions/reconciliation-idempotency.md), [baseline-counterfactual-metric](decisions/baseline-counterfactual-metric.md) |
| Product vision & phases | CLAUDE.md → *What we're building*; `roadmap__small.pdf` | not duplicated here |
| Observability | events → Postgres; errors → Sentry; logs → pino/stdout; traces deferred | [observability-stack](decisions/observability-stack.md); rules in CLAUDE.md |
| Kill switches / fail-closed / idempotency | `feature_flags` table + `isFeatureEnabled()`; rules in CLAUDE.md → *Safety infrastructure* | [feature-flags-and-kill-switches](decisions/feature-flags-and-kill-switches.md) |
| Schema / migrations / tests / DB host | Drizzle + drizzle-kit, Vitest, Neon (pooled WebSocket driver) | [migration-and-test-tooling](decisions/migration-and-test-tooling.md) |
| Operator (admin) access | A hand-rolled shared-secret gate — a deliberate, temporary exception to CLAUDE.md → *Architecture*'s "use a library for auth". Frozen: security fixes only, no new surfaces, replaced by a maintained auth library once a second operator, any role distinction, a mutating admin surface, or a non-Vercel deploy appears. Entirely separate from the SIWS user session — "has a wallet session" is never a proxy for "is an operator" | [admin-auth-is-provisional](decisions/admin-auth-is-provisional.md), [admin-metrics-secret-gate](decisions/admin-metrics-secret-gate.md) |
| Caller identity | `resolveSession()` is the **only** answer to "which wallet is this request for" — no route reads an address from a request body; single-use nonce, supersede+consume+insert in one transaction, 30-day sliding expiry inside a 90-day absolute cap | [auth README](../apps/web/src/server/auth/README.md) |
| Unauthenticated write surfaces | `/api/auth/nonce` is throttled by counting its own rows in Postgres (10 / 5 min, keyed by a hashed client address) and reaped opportunistically — no scheduler and no Redis until a scheduled worker exists (none does: reconciliation runs in-request on app open). The key is an **unverified forwarded header**: a deployment trust assumption, not a bug | [auth README](../apps/web/src/server/auth/README.md), [rate-limit-forwarded-header-trust](decisions/rate-limit-forwarded-header-trust.md) |
| Authenticated write surfaces | constitution event *writes* are throttled per `userId` against `events` — a **deliberate second copy** of the challenge limiter's shape (its query is bound to `siws_challenges`), sharing only its constants. Phase 8 reuses the same primitive as an **action gate**, not only a write guard: an increase request is rejected outright once throttled, counted by *attempt* (not success, which missed repeated `pending_change_exists`-rejected tries) — but only the increase path; a decrease or a cancel-pending never calls it, and neither can ever be blocked by it. Phase 9's `server/feedback/feedback.ts` is the third consumer (`feedback.submitted` only — `feedback.prompt_shown` is deliberately exempt, see the `feedback` row above — as an action gate identical to Phase 8's shape) — the "extract on a third consumer" note from earlier phases was consciously deferred rather than acted on: `constitution/rate-limit.ts` is imported as-is (still generic — `userId`/`eventType`/`correlationId`/`now`, nothing constitution-specific in its implementation), and a physical move/rename was judged higher-risk than reusing it in place for this remediation pass. `server/admin/login-rate-limit.ts` is a related but structurally different case (unauthenticated, no `userId` to key on) — a sibling limiter against its own table (`admin_login_attempts`), not a consumer of this one. Revisit if a fourth consumer of *this* limiter appears | [constitution README](../apps/web/src/server/constitution/README.md), [asymmetric-constitution-edits](decisions/asymmetric-constitution-edits.md) |
| Wallet identity | session identity can lag the wallet's active account after an in-extension switch — **accepted limitation**, read before using `resolveSession()` without a fresh signature | [wallet-account-switch-desync](decisions/wallet-account-switch-desync.md) |
