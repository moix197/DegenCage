# Copy to `.env` at the repo root. `.env` is gitignored — never commit real values.
# Root-level scripts (`pnpm dev`, `pnpm build`, `pnpm db:*`) load this file via dotenv-cli.

# Postgres (Neon). Two URLs on purpose:
#   DATABASE_URL        direct connection — drizzle-kit generate/migrate only.
#   DATABASE_URL_POOLED pooled connection — everything the app does at runtime.
# Serverless opens a connection per invocation, so runtime never uses the direct URL.
DATABASE_URL=postgresql://user:password@host.neon.tech/degencage?sslmode=require
DATABASE_URL_POOLED=postgresql://user:password@host-pooler.neon.tech/degencage?sslmode=require

# Error tracking. Read at startup by `src/instrumentation.ts` (server/edge) and
# `src/instrumentation-client.ts` (browser, which can only see NEXT_PUBLIC_ vars, and
# which fetches the SDK only when this is set — it is inlined at build time).
# Unset = Sentry stays uninitialised; a startup warning says so and the wrapper still logs.
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=

# Log level for the pino wrapper: trace|debug|info|warn|error|fatal.
LOG_LEVEL=info

# The domain a Sign In With Solana message must be bound to — e.g. `localhost:3000` in
# dev, the production host otherwise. Configuration only: it is never derived from a
# request header, or an attacker who can set `Host`/`Origin` chooses what they sign for.
SIWS_DOMAIN=localhost:3000

# Helius — the only source of wallet transaction history (`getTransactionsForAddress`).
# Unset = reconciliation cannot pull history; the wallet's trades never populate.
HELIUS_API_KEY=

# Birdeye — long-tail (alt<->alt) price fallback only, consulted when neither leg of a
# swap is a stablecoin or SOL. Gated by the `pricing.birdeye` flag as well as this key.
# Unset = those swaps resolve to `usd_value: null` (fail closed, never a guessed price),
# and each attempt reports one error to Sentry — set the key or turn the flag off.
BIRDEYE_API_KEY=

# Shared secret gating GET /api/admin/metrics (Phase 9's internal-only success-signals
# route — no RBAC system exists yet, decision 11). Sent as the `x-admin-metrics-secret`
# header. Unset = the route answers 404 for every caller, never 200 — there is no valid
# secret to compare against, so it fails closed the same way a wrong secret does.
ADMIN_METRICS_SECRET=
